# 服务器启动检查清单

## ✅ 启动前准备

### 1. 确认网络环境
```bash
# 获取本机IP（应该是 192.168.1.3）
ifconfig | grep "inet " | grep -v 127.0.0.1

# 确认路由器连接
ping -c 3 192.168.1.1
```

### 2. 确认端口可用
```bash
# 检查3000端口是否被占用
lsof -i :3000

# 如果有输出，需要先停止占用进程
```

### 3. 确认依赖安装
```bash
cd /path/to/超协体MVP1.0代码库
ls node_modules

# 如果没有 node_modules，执行：
npm install
```

### 4. 确认数据库配置
```bash
cp .env.example .env
# 在 .env 中填写 DATABASE_URL 和 JWT_SECRET
```

### 5. 环境检查（可选）
```bash
npm run check:env
```

---

## 🚀 启动服务器

### 方式1: 使用启动脚本（推荐）
```bash
cd /path/to/超协体MVP1.0代码库
./start-server.sh
```

**优点**: 自动防止Mac休眠，显示IP地址

### 方式2: 直接启动
```bash
cd /path/to/超协体MVP1.0代码库
npm start
```

**注意**: 需要手动执行防休眠命令

---

## ✅ 启动后验证

### 1. 本地健康检查
```bash
curl http://localhost:3000/health
```

**预期输出**:
```json
{
  "status": "healthy",
  "timestamp": "2026-01-20T...",
  "stats": {
    "tasks": 2,
    "members": 2
  }
}
```

### 2. 局域网健康检查
```bash
curl http://192.168.1.3:3000/health
```

**预期**: 同样返回healthy状态

### 3. MCP端点检查
```bash
curl http://192.168.1.3:3000/mcp/manifest | jq .
```

**预期**: 返回10个工具的定义

### 4. 使用其他设备测试（可选）
```bash
# 在手机/iPad浏览器访问
http://192.168.1.3:3000/health

# 应该看到JSON响应
```

---

## 🔄 服务器运行中

### 保持Mac唤醒
如果使用 `npm start` 启动，需要在另一个终端执行：
```bash
caffeinate -i
```

### 查看实时日志
服务器日志会直接显示在启动终端，包括：
- 所有API调用
- 成员注册/任务创建事件
- 错误信息（如果有）

### 监控连接
```bash
# 查看当前连接数
lsof -i :3000 | grep ESTABLISHED
```

---

## 🛑 停止服务器

在服务器终端按 `Ctrl+C`

**确认停止**:
```bash
curl http://localhost:3000/health
# 应该返回：curl: (7) Failed to connect
```

---

## ❓ 故障排查

### 问题1: 端口被占用
```bash
# 查找占用进程
lsof -i :3000

# 停止进程（替换PID）
kill -9 [PID]
```

### 问题2: 局域网无法访问
```bash
# 检查防火墙
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate

# 如果开启，需要允许Node.js
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblock /usr/local/bin/node
```

### 问题3: Mac自动休眠
```bash
# 临时禁用休眠（推荐使用 start-server.sh）
caffeinate -i npm start

# 永久修改休眠设置
# 系统偏好设置 → 节能 → 防止Mac自动进入睡眠（接通电源时）
```

### 问题4: IP地址变化
```bash
# 设置静态IP
# 系统偏好设置 → 网络 → Wi-Fi → 高级 → TCP/IP
# 配置IPv4: 手动
# IPv4地址: 192.168.1.3
# 子网掩码: 255.255.255.0
# 路由器: 192.168.1.1
```

---

## 📊 运行状态监控

### 快速检查命令
```bash
# 健康状态
curl -s http://localhost:3000/health | jq .

# 成员数量
curl -s -X POST http://localhost:3000/mcp/tools/call \
  -H 'Content-Type: application/json' \
  -d '{"tool":"list_all_members","arguments":{}}' \
  | jq -r '.content[0].text' | jq '.members | length'

# 任务数量
curl -s -X POST http://localhost:3000/mcp/tools/call \
  -H 'Content-Type: application/json' \
  -d '{"tool":"list_all_tasks","arguments":{}}' \
  | jq -r '.content[0].text' | jq '.tasks | length'
```

---

## 🎯 日常运行建议

### 工作日（9:00-22:00）
1. 早上启动服务器
2. 保持终端窗口开启
3. Mac接通电源
4. 晚上下班前可选择停止

### 周末/非工作时间
- 可以停止服务器节省电力
- 如果有成员需要访问，提前告知

### 未来升级（可选）
- 树莓派4B：24小时运行，月电费<5元
- PM2守护进程：自动重启，日志管理
- Docker容器：隔离环境，便于迁移

---

## ✅ 准备邀请邻居

确认以下项目都✅后再发送邀请：

- [ ] 服务器成功启动
- [ ] 本地健康检查通过
- [ ] 局域网健康检查通过
- [ ] MCP端点可访问
- [ ] Mac防休眠配置完成
- [ ] IP地址固定（或记录当前IP）
- [ ] 邀请消息准备好（填写门牌号/微信）
- [ ] 确定接入派对时间（可选）

**全部完成？恭喜！可以开始邀请邻居了 🎉**

邀请消息模板：
- 详细版：`邀请消息.md`
- 微信版：`快速邀请-微信版.txt`
