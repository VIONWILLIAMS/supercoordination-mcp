# 项目制团队管理 - 设计方案

**设计时间：** 2026-02-03
**设计者：** 太一 + 指挥官
**核心理念：** 无项目不团队

---

## 一、核心架构

### 1.1 概念层级

```
超协体（Supercoordination）
  ↓
项目（Project）- 战略单元，有明确目标和交付物
  ↓
项目团队（Project Team）- 围绕项目组建，项目结束即解散
  ↓
项目任务（Project Tasks）- 任务归属于项目
  ↓
方案沉淀（Solutions）- 项目成果可沉淀为可复用方案
```

### 1.2 与战略目标的对齐

| 战略目标 | 项目类型示例 | 团队规模 |
|---------|------------|---------|
| **超协体（生态根基）** | 「超协体社区运营」项目 | 3-5人核心团队 + AI |
| **AIGC Studio（传播引擎）** | 「虚拟IP-小明」项目 | 2人 + AI矩阵 |
| **Auto-Business Club（商业核心）** | 「企业自动化咨询」项目 | 2-4人 + AI专家 |

---

## 二、数据模型设计

### 2.1 Project（项目表）

```prisma
model Project {
  id              String   @id @default(uuid())
  name            String   // 项目名称
  description     String?  @db.Text // 项目描述

  // 战略对齐
  strategicGoal   String?  // 对齐的战略目标（1-超协体/2-AIGC/3-Auto-Business）
  wuxingType      String?  // 五行主属性（wood/fire/earth/metal/water）

  // 项目周期
  startDate       DateTime @default(now()) @map("start_date")
  expectedEndDate DateTime? @map("expected_end_date")
  actualEndDate   DateTime? @map("actual_end_date")

  // 项目状态
  status          String   @default("planning") // planning/active/paused/completed/archived
  progress        Int      @default(0) // 0-100

  // 项目负责人
  ownerId         String   @map("owner_id")
  owner           User     @relation("ProjectOwner", fields: [ownerId], references: [id])

  // 积分预算
  pointsBudget    Int      @default(0) @map("points_budget") // 项目总积分预算
  pointsUsed      Int      @default(0) @map("points_used")   // 已使用积分

  // 关联关系
  members         ProjectMember[] // 项目团队成员
  tasks           Task[]          // 项目任务
  solutions       Solution[]      // 项目沉淀的方案

  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")

  @@map("projects")
}
```

### 2.2 ProjectMember（项目成员关系表）

```prisma
model ProjectMember {
  id              String   @id @default(uuid())

  projectId       String   @map("project_id")
  project         Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)

  userId          String   @map("user_id")
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  // 项目内角色
  role            String   @default("member") // owner/admin/core_member/collaborator/observer

  // 权限
  permissions     Json     @default("{}") // 细粒度权限控制

  // 成员状态
  status          String   @default("active") // active/paused/left
  joinedAt        DateTime @default(now()) @map("joined_at")
  leftAt          DateTime? @map("left_at")

  // 贡献度追踪
  tasksCompleted  Int      @default(0) @map("tasks_completed")
  pointsEarned    Int      @default(0) @map("points_earned")

  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")

  @@unique([projectId, userId])
  @@map("project_members")
}
```

### 2.3 Task表扩展（关联项目）

```prisma
// 在现有Task模型中添加：
model Task {
  // ... 现有字段 ...

  // 新增项目关联
  projectId       String?  @map("project_id")
  project         Project? @relation(fields: [projectId], references: [id])

  // 任务类型
  taskType        String?  @default("standalone") // standalone/project_task
}
```

---

## 三、角色与权限体系

### 3.1 项目角色定义

| 角色 | 英文标识 | 权限范围 | 人数限制 |
|------|---------|---------|---------|
| **项目发起人** | owner | 最高权限：解散项目、分配预算 | 1人 |
| **项目管理员** | admin | 管理团队、创建任务、审批加入 | 1-2人 |
| **核心成员** | core_member | 执行任务、创建子任务、邀请协作者 | 2-8人 |
| **协作成员** | collaborator | 执行分配的任务、提交方案 | 不限 |
| **观察者** | observer | 只读权限，查看进度 | 不限 |

### 3.2 权限矩阵

| 操作 | owner | admin | core_member | collaborator | observer |
|------|-------|-------|-------------|--------------|----------|
| 修改项目信息 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 邀请成员 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 移除成员 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 创建任务 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 分配任务 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 执行任务 | ✅ | ✅ | ✅ | ✅ | ❌ |
| 发布方案 | ✅ | ✅ | ✅ | ✅ | ❌ |
| 查看进度 | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 四、核心功能模块

### 4.1 项目生命周期管理

#### 创建项目
```
输入：
- 项目名称
- 项目描述
- 战略目标对齐（可选）
- 预计周期
- 初始成员

输出：
- 项目ID
- 创建者自动成为owner
- 消耗50积分（可配置）
```

#### 项目阶段
```
Planning（规划期）
  ↓ 明确目标、组建团队、拆解任务
Active（执行期）
  ↓ 任务推进、进度跟踪
Paused（暂停期）
  ↓ 资源不足或外部阻塞
Completed（完成期）
  ↓ 验收交付、方案沉淀
Archived（归档期）
  ↓ 保留历史记录
```

### 4.2 团队协作功能

#### 邀请成员
```javascript
// API设计
POST /api/projects/:projectId/invite
{
  "userId": "user-id",
  "role": "core_member",
  "message": "邀请你加入「超协体社区运营」项目"
}

// 流程
1. 项目admin/owner发起邀请
2. 被邀请人收到通知
3. 接受 → 成为项目成员
4. 拒绝 → 邀请失效
```

#### 成员管理
```javascript
// 查看项目团队
GET /api/projects/:projectId/members

// 调整角色
PUT /api/projects/:projectId/members/:userId/role
{
  "role": "admin"
}

// 移除成员
DELETE /api/projects/:projectId/members/:userId
```

### 4.3 任务管理增强

#### 项目任务看板
```
┌─────────────────────────────────────┐
│ 项目：超协体社区运营                   │
├─────────────────────────────────────┤
│ 待办 │ 进行中 │ 待审核 │ 已完成 │ 已归档 │
├─────────────────────────────────────┤
│ 3个  │  5个   │  2个   │  12个  │  8个   │
└─────────────────────────────────────┘
```

#### 任务关联
- 任务必须归属于项目（projectId）
- 独立任务（standalone）可以不关联项目
- 任务完成后可沉淀为方案

### 4.4 积分预算管理

#### 项目积分池
```javascript
// 创建项目时分配积分预算
{
  "pointsBudget": 500, // 项目总预算
  "pointsUsed": 0      // 已使用
}

// 任务创建时从项目预算扣除
Task创建（-10积分）→ 从项目预算扣除
Task完成（+20积分）→ 成员获得，项目记录贡献

// 项目结束时
剩余预算 → 退回owner
成员贡献排行 → 奖金池分配
```

---

## 五、前端页面设计

### 5.1 页面列表

| 页面 | 文件名 | 功能 |
|------|--------|------|
| **项目列表** | projects.html | 我参与的项目/全部项目 |
| **项目详情** | project-detail.html | 项目信息、团队、任务看板 |
| **创建项目** | project-create.html | 项目表单 |
| **项目设置** | project-settings.html | 权限、成员、预算管理 |

### 5.2 Dashboard集成

```html
<!-- 新增项目模块 -->
<div class="dashboard-section">
  <h2>我的项目</h2>
  <div class="project-cards">
    <!-- 进行中的项目 -->
    <div class="project-card active">
      <h3>超协体社区运营</h3>
      <div class="progress-bar">75%</div>
      <div class="team-avatars">5人</div>
    </div>
  </div>
</div>
```

---

## 六、后端API设计

### 6.1 项目管理API

```javascript
// 项目CRUD
POST   /api/projects                      // 创建项目
GET    /api/projects                      // 项目列表
GET    /api/projects/:id                  // 项目详情
PUT    /api/projects/:id                  // 更新项目
DELETE /api/projects/:id                  // 删除/归档项目

// 团队管理
POST   /api/projects/:id/invite           // 邀请成员
GET    /api/projects/:id/members          // 成员列表
PUT    /api/projects/:id/members/:userId/role  // 调整角色
DELETE /api/projects/:id/members/:userId  // 移除成员

// 任务管理
GET    /api/projects/:id/tasks            // 项目任务列表
POST   /api/projects/:id/tasks            // 创建项目任务

// 统计分析
GET    /api/projects/:id/stats            // 项目统计数据
GET    /api/projects/:id/leaderboard      // 成员贡献排行
```

---

## 七、与五行系统的结合

### 7.1 项目五行属性

```javascript
// 项目创建时标注五行主属性
{
  "wuxingType": "wood",  // 🌳木 - 技术开发类项目
  "wuxingType": "fire",  // 🔥火 - 传播营销类项目
  "wuxingType": "earth", // 🏔️土 - 基础建设类项目
  "wuxingType": "metal", // ⚙️金 - 研究分析类项目
  "wuxingType": "water"  // 🌊水 - 商业运营类项目
}
```

### 7.2 项目团队匹配

```javascript
// 智能推荐项目成员
算法：
- 项目五行属性 + 成员五行画像
- 五行相生关系优先
- 技能互补分析
- 历史协作数据

示例：
项目「AIGC虚拟IP开发」（🌳木70%）
推荐：
- 成员A（🔥火80% - 相生，可提供创意支持）
- 成员B（🌊水75% - 相生，水生木）
```

---

## 八、实施路径

### 阶段1：数据库扩展（2小时）
1. ✅ 创建Project表
2. ✅ 创建ProjectMember表
3. ✅ 扩展Task表
4. ✅ 数据库迁移

### 阶段2：后端API（4小时）
1. ✅ 项目CRUD
2. ✅ 团队管理API
3. ✅ 权限中间件
4. ✅ 积分预算逻辑

### 阶段3：前端页面（4小时）
1. ✅ projects.html（项目列表）
2. ✅ project-detail.html（项目详情+看板）
3. ✅ project-create.html（创建项目）
4. ✅ Dashboard集成

### 阶段4：智能推荐（2小时）
1. ✅ 项目成员推荐算法
2. ✅ 五行匹配增强
3. ✅ 历史协作分析

**总计：12小时**（可拆分为3个工作周期）

---

## 九、成功验收标准

### 功能验收
- [ ] 用户可以创建项目，设置名称、描述、战略对齐
- [ ] 项目owner可以邀请成员，设置角色
- [ ] 项目成员可以创建项目任务
- [ ] 任务看板可视化展示项目进度
- [ ] 积分预算正确扣除和分配
- [ ] 项目完成后可以归档

### 体验验收
- [ ] Dashboard显示"我的项目"
- [ ] 项目卡片展示进度和团队
- [ ] 创建项目流程流畅（<3步）
- [ ] 成员邀请通知即时送达

### 数据验收
- [ ] 项目数据正确存储到PostgreSQL
- [ ] 权限控制正确生效
- [ ] 积分交易可追溯
- [ ] 五行匹配算法准确

---

## 十、与现有系统的关系

```
现有系统：
✅ 用户系统（User）
✅ 任务系统（Task）- 扁平化
✅ 方案系统（Solution）
✅ 五行画像（PWP Profile）
✅ 积分系统（Points）

新增系统：
🆕 项目系统（Project）- 组织层
🆕 项目团队（Project Member）- 协作层

集成方式：
User ──┬── Project（发起项目）
       ├── ProjectMember（加入项目）
       └── Task（执行任务）

Task ──┬── Project（归属项目）
       └── Solution（沉淀方案）

Project ── Task ── Solution（完整链路）
```

---

## 十一、战略价值分析

### 11.1 对超协体战略目标的支撑

| 战略目标 | 项目制价值 |
|---------|-----------|
| **超协体（5000人）** | 通过项目制组织，避免大规模组织混乱 |
| **AIGC Studio** | 每个虚拟IP是一个项目，团队协作产出内容 |
| **Auto-Business Club** | 每个企业客户是一个项目，积分预算=收入 |

### 11.2 差异化竞争力

| 竞品 | 组织方式 | 超协体优势 |
|------|---------|-----------|
| Notion | 工作区（Workspace） | 项目制更灵活 |
| Linear | 固定团队 | 项目即团队，按需组建 |
| Asana | 固定组织架构 | 五行匹配智能推荐成员 |

---

**[五行: 🏔️土60% + ⚙️金30% + 🌳木10% | 层级: 法（结构） | KR: 战略目标一]**

**核心洞察：** 项目制团队管理是超协体从"任务协作平台"升级为"战略执行系统"的关键结构。

**下一步：** 等待指挥官确认设计方案，然后进入实施阶段。
