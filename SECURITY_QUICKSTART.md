# 超协体安全快速指南

## ⚠️ 当前安全状态

```
风险等级: 🟡 中等（可控）
防护状态: ✅ 备份已启用
权限控制: ❌ 未实施（规划中）
```

## 🛡️ 已启用的保护措施

### 1. 自动备份系统 ✅

**位置**: `~/ClaudeWorkspace/supercoordination-mcp/backups/`
**频率**: 手动（可设置定时任务）
**保留**: 最近 50 个备份

**手动备份**：
```bash
cd ~/ClaudeWorkspace/supercoordination-mcp
./backup-data.sh
```

**恢复数据**（如果被误删/篡改）：
```bash
# 1. 停止 MCP 服务器
lsof -ti:3000 | xargs kill

# 2. 查看备份列表
ls -lt ~/ClaudeWorkspace/supercoordination-mcp/backups/

# 3. 恢复指定备份
cp ~/ClaudeWorkspace/supercoordination-mcp/backups/store_20260131_083506.json \
   ~/ClaudeWorkspace/supercoordination-mcp/data/store.json

# 4. 重启服务器
cd ~/ClaudeWorkspace/supercoordination-mcp
npm start
```

---

## 🚨 如果数据被篡改怎么办？

### 检测篡改

```bash
# 查看当前数据
cat ~/ClaudeWorkspace/supercoordination-mcp/data/store.json | jq .

# 对比最近备份
diff <(cat ~/ClaudeWorkspace/supercoordination-mcp/data/store.json | jq .) \
     <(cat ~/ClaudeWorkspace/supercoordination-mcp/backups/store_*.json | head -1 | jq .)
```

### 快速恢复

**3步恢复**：
1. 停止服务器（`lsof -ti:3000 | xargs kill`）
2. 恢复备份（见上方命令）
3. 重启服务器

**时间成本**: < 1分钟

---

## 🤝 当前推荐的协作规范

### 数据命名规范

**成员命名**：
- ✅ 好：`OpenCode_Alice`、`Claude_Bob`
- ❌ 坏：`Alice`、`测试用户`

**任务命名**：
- ✅ 好：`[Alice创建] 开发登录API`
- ❌ 坏：`开发API`

### 操作规范

| 操作 | 允许 | 禁止 |
|------|------|------|
| **创建成员** | ✅ 创建自己的代理 | ❌ 冒充他人 |
| **创建任务** | ✅ 自由创建 | ❌ 垃圾数据 |
| **更新任务** | ✅ 自己创建的 | ⚠️ 他人创建的需沟通 |
| **分配任务** | ✅ 分配给自己 | ⚠️ 分配给他人需确认 |

### 冲突解决流程

**场景1**：发现数据被意外修改
```
1. 立即备份当前状态
2. 在团队群沟通确认
3. 达成一致后恢复或保留
```

**场景2**：同时创建了重复数据
```
1. 保留创建时间早的
2. 或合并两者信息
3. 删除重复项（待有删除功能后）
```

---

## 📅 安全升级路线图

### 本周（已完成）
- ✅ 自动备份系统
- ✅ 安全方案文档

### 下周
- ⏳ 添加操作日志（记录谁做了什么）
- ⏳ 限制网络访问（仅 localhost 或 VPN）

### 2-4周
- 🎯 API Key 身份验证
- 🎯 基础权限系统（Owner/Admin/Member/Viewer）
- 🎯 数据所有权（created_by 字段）

### 3-6个月
- 🎯 区块链式审计日志
- 🎯 智能合约规则引擎

---

## 🆘 紧急联系方式

**数据问题**：
- 查看日志：`tail -f /private/tmp/claude/-Users-personalworkplacce/tasks/b284a5d.output`
- 查看备份：`ls -lt ~/ClaudeWorkspace/supercoordination-mcp/backups/`

**服务器问题**：
- 健康检查：`curl http://localhost:3000/health`
- 重启服务器：`cd ~/ClaudeWorkspace/supercoordination-mcp && npm start`

---

## 💡 最佳实践

### 每日检查清单

```bash
# 1. 检查服务器状态
curl -s http://localhost:3000/health | jq .

# 2. 查看成员数量（是否异常）
curl -s http://localhost:3000/health | jq '.stats.members'

# 3. 手动备份
cd ~/ClaudeWorkspace/supercoordination-mcp && ./backup-data.sh
```

### 每周检查清单

```bash
# 1. 清理旧备份（保留50个）
ls ~/ClaudeWorkspace/supercoordination-mcp/backups/ | wc -l

# 2. 查看数据增长
du -sh ~/ClaudeWorkspace/supercoordination-mcp/data/

# 3. 验证数据完整性
cat ~/ClaudeWorkspace/supercoordination-mcp/data/store.json | jq empty
```

---

**[🏔️土 | 器]**

**记住**：备份是最后一道防线，定期备份 = 心安！

当前方案足够应对小团队协作，待用户增多再升级权限系统。
